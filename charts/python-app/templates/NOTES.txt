Application {{ .Release.Name }} deployed successfully.

  Namespace: {{ .Release.Namespace }}
  Replicas:  {{ .Values.replicaCount }}

{{- if .Values.ingress.enabled }}
  Access via ingress:
  {{- range .Values.ingress.hosts }}
    {{- range .paths }}
    http://LOAD_BALANCER_IP{{ .path }}
    {{- end }}
  {{- end }}
{{- else }}
  Port forward:
    kubectl port-forward svc/{{ .Values.service.name }} {{ .Values.service.port }}:{{ .Values.service.port }} -n {{ .Release.Namespace }}
{{- end }}
